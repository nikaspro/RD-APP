/** Точка входа логики виджета. Идемпотентна: повторный вызов ничего не делает. */
export declare function initChiposh(): void

/** Мостик наружу: React вызывает те же функции рантайма, что и раньше. */
export declare const chiposh: {
  /** Показать панель по ключу таба. */
  show: (key: string) => void
  /** Закрыть лист разговора (учитывает режим набора текста). */
  closeSheet: () => void
}
